[package]
name = "kernel"
version = "0.1.0"
edition = "2021"
authors = ["ZaminOS"]
description = "ZaminOS kernel — Rust, aarch64, mobile-first konvergent OS"

[dependencies]

[[bin]]
name = "kernel"
path = "src/main.rs"
test = false
bench = false
